{
  "name": "@nikosdouvlis/js",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx turbo run build",
    "clean": "npx turbo run clean",
    "test": "echo \"Error: no test specified\" && exit 1",
    "changeset": "npx changeset",
    "version": "npx changeset version",
    "version:snapshot": "npx changeset version --no-git-tag --snapshot snap",
    "version:staging": "npx changeset version --no-git-tag --snapshot staging",
    "release": "npm run build && npx changeset publish && git push --follow-tags"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "turbo": "^1.8.3",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.4.8"
  }
}
